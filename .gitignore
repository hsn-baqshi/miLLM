.env

# Local Python venv (training)
.venv-train/
.venv-train-wsl/

# Training (LlamaFactory / W&B / TensorBoard)
wandb/
training/saves/
training/runs/
training/data/*.jsonl

# Outlook MCP / MSAL token cache (default path next to graph_session when MSAL_TOKEN_CACHE_PATH unset)
**/integrations/outlook_mcp/token_cache.bin

# Open WebUI local DB copy / feedback export (may contain private data)
webui-temp.db
openwebui-feedback-export.json
